<Window x:Class="ritchell.library.ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="300"
        Width="300"
        Title="MVVM Light Application"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        WindowState="Maximized">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <DockPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" DockPanel.Dock="Top">
                <Button Content="Book Sections" 
                    Command="{Binding NavigateToPageCommand}" 
                    CommandParameter="SectionPage.xaml"/>
                <Button Content="Books" 
                        Command="{Binding NavigateToPageCommand}" 
                        CommandParameter="BookPage.xaml"/>
                <Button Content="RFID" />
            </StackPanel>
            <Frame DockPanel.Dock="Bottom" x:Name="LayoutRoot" NavigationUIVisibility="Hidden"/>
        </DockPanel>
    </Grid>
</Window>
