<UserControl x:Class="ritchell.library.ui.client.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ritchell.library.ui.client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             DataContext="{Binding DashboardViewModel,Source={StaticResource Locator}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Left">
            <Button Command="{Binding BorrowReturnBookCommand}" Style="{DynamicResource ButtonStyle2}">Borrow / Return Books</Button>
            <Button Style="{DynamicResource ButtonStyle2}">Transaction History</Button>
            <Button Style="{DynamicResource ButtonStyle2}">Logout</Button>
        </StackPanel>

        <ContentPresenter Content="{Binding DashboardContent}" 
                          Grid.Column="1"/>
    </Grid>
</UserControl>
