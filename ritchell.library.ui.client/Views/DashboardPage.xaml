<UserControl x:Class="ritchell.library.ui.client.Views.DashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ritchell.library.ui.client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Vertical">
            <Button>Borrow / Return Books</Button>
            <Button>Transaction History</Button>
            <Button>Logout</Button>
        </StackPanel>
        <GridSplitter Grid.Column="1" 
                      BorderThickness="1"/>
        
        <ContentPresenter Content="{Binding DashboardContent}" 
                          Grid.Column="2"/>
    </Grid>
</UserControl>
