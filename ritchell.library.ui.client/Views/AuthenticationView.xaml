<UserControl x:Class="ritchell.library.ui.client.Views.AuthenticationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ritchell.library.ui.client.Views"
      mc:Ignorable="d"  d:DesignHeight="65.334" d:DesignWidth="553.917"
      xmlns:wpf="clr-namespace:ritchell.library.ui.client.WPF"
      DataContext="{Binding AuthenticationViewModel, Source={StaticResource Locator}}">

    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <ContentPresenter>
                <ContentPresenter.ContentTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Fullname}"/>
                    </DataTemplate>
                </ContentPresenter.ContentTemplate>
            </ContentPresenter>
            <TextBlock Margin="6,0" FontSize="16">Username</TextBlock>
            <TextBox Text="{Binding Username,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                     MinWidth="140" Margin="6,0" FontSize="16"/>
            <TextBlock Margin="6,0" FontSize="16">Password</TextBlock>
            <PasswordBox MinWidth="140"
                         wpf:PasswordHelper.Attach="True" 
                         wpf:PasswordHelper.Password="{Binding Password,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="6,0" FontSize="16"/>
            <Button Command="{Binding LoginCommand}" Margin="6,0" FontSize="16" Padding="7,3" Style="{DynamicResource ButtonStyle1}">Login</Button>
        </StackPanel>
    </Grid>
</UserControl>
